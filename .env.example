# Frontend EcoClasificador — variables de entorno

# URL del endpoint POST /api/v1/predict de la API backend.
# En prod apunta al backend público (saltaget.com.ar). En dev podés dejar localhost:8000.
NEXT_PUBLIC_API_URL=https://saltaget.com.ar/api/v1/predict
