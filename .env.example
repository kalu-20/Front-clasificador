# Frontend EcoClasificador — variables de entorno

# URL del endpoint POST /api/v1/predict de la API backend.
# En prod debería apuntar a Railway. En dev podés dejar localhost:8000.
NEXT_PUBLIC_API_URL=https://ecoclasificador-api-production.up.railway.app/api/v1/predict
